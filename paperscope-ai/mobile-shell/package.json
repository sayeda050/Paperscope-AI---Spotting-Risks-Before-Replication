{
  "name": "paperscope-mobile-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Android WebView shell for PaperScope AI",
  "scripts": {
    "android": "npx cap open android",
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "build-android": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@types/node": "^22.15.3",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}